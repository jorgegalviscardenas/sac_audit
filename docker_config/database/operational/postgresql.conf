# PostgreSQL configuration for operational database

# Connection Settings
listen_addresses = '*'
max_connections = 100

# Memory Settings
shared_buffers = 2GB

# Write Ahead Log
checkpoint_timeout = 10min
