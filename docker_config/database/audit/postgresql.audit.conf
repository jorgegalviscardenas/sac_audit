# PostgreSQL configuration for audit database

# Connection Settings
listen_addresses = '*'
max_connections = 100

# Memory Settings
shared_buffers = 4GB

# Write Ahead Log
synchronous_commit = off
checkpoint_timeout = 15min
wal_buffers = 64MB
